# Exact-head deploy boundary

This branch only builds the deployment control plane. It does not change PR #30, merge either PR, restart production, create a live payment, or claim the Hosted-v1 reality gate has passed.
